extends base.pug

block content
    h2 #{video.title} 제목 수정
    form(method="POST")
        input(name="potato", placeholder="Video Title", value=video.title, required)
        input(value="Save", type="submit")