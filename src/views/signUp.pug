extends base.pug

block content
    form(id="test3", method="POST")
            input(name="username", type="text", placeholder="username", required)
            input(type="submit", value="check!")
        
    form(id="test", method="POST")
        if errorMessage
            span #{errorMessage}
        br
        input(name="name", type="text", placeholder="name", required)
        br
        input(name="email", type="email", placeholder="email", required)
        br
        br
        input(name="password", type="password", placeholder="password", required)
        br
        input(placeholder="Confirm Password", name="password2", type="password", required)
        br
        input(name="location", type="text", placeholder="location", required)
        br
        input(type="submit", value="Join!")
    button(name="check", type="button", onclick="func()") 아이디 확인
      
    hr
    div
        span Already have an account? 
        a(href="/login") Log in now &rarr;
    script.
        const val = #{link};
        const func = function(){console.log(val)};
        //- const testForm = document.querySelector("#test2");
        //- testForm.addEventListener("submit", func);