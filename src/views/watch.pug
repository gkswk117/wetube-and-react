extends base.pug

block content
  video(src="/"+video.fileUrl, controls width="500")
  div
    button#play Play
    button#mute Mute 
    span#time 00:00/00:00
    input(type="range", step="0.1", value="0.5", min="0", max="1")#volume
  
  //- div
  //-   p Watch | #{video.title}
  //-   small #{video.meta.views} #{video.meta.views===1 || video.meta.views===0 ? "view" : "views"} 
  //-   br
  //-   small Uploaded by 
  //-     a(href=`/user/${video.owner._id}`) #{video.owner.name}
  //- hr
  //- if String(video.owner._id) === String(loggedInUser._id)
  //-   a(href=`${video.id}/edit`) Edit Video &rarr;
  //-   br
  //-   a(href=`${video.id}/delete`) Delete Video &rarr;

block scripts
  script(src="/test/js/videoPlayer.js")